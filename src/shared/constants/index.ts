import { IPC } from './ipc'
import { PATHS } from './paths'

const constants = {
  WINDOW_BOUNDS: {
    WIDTH: 1280,
    HEIGHT: 800
  },
  WINDOW_MIN_BOUNDS: {
    WIDTH: 640,
    HEIGHT: 400
  },
  IPC,
  PATHS
}

export default constants
