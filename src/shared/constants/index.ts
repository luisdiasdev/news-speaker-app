import { IPC } from './ipc'
import { PATHS } from './paths'

const constants = {
  WindowBounds: {
    width: 1280,
    height: 800
  },
  WindowMinBounds: {
    width: 640,
    height: 400
  },
  IPC,
  PATHS
}

export default constants
