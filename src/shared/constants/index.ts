import { IPC } from './ipc'
import { PATHS } from './paths'

const constants = {
  WindowBounds: {
    width: 800,
    height: 600
  },
  WindowMinBounds: {
    width: 400,
    height: 300
  },
  IPC,
  PATHS
}

export default constants
